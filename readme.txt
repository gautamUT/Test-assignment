init script for mysql- src/com/sectors/script (create db with name "db", tables, init data)

src/com/sectors/listener - init mysql datasource with login and password
		dataSource.setUser("root");
		dataSource.setPassword("pass");